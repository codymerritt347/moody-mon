<h1>Congrats, <%= current_user.name %>!</h1>

<h3>Since you created an entry, you're awarded 10 coins!</h3>

<h3>You now have <%= current_user.coins %> coins</h3>

<h3><%= current_user.monster.name %> gains 15 experience points!</h3>

<a href="/users/<%= current_user.id %>">Your Homepage</a>